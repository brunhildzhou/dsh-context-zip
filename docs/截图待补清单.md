# 截图待补清单

`README.md` 与 `README.en.md` 的截图区当前是占位。本清单列出要补的图：全部须是在隐藏虚拟桌面里对真实运行的 DSH 截到的真图，不画假图，不用文字冒充截图。

归档位置：`docs/images/`。引用时用相对路径，例如 `docs/images/panel-wire-active.png`。

| 编号 | 内容 | 状态 |
|---|---|---|
| 1 | 设置面板 ContextZip 一节，「压缩后端」行：未生效态 | 待补 |
| 2 | 同一行：正在接管态 | 待补 |
| 3 | 同一行：已生效态 | 待补 |
| 4 | 同一行：等待重启态 | 待补 |
| 5 | 检索工具输出示例（`history_segments` 或 `history_read` 的真实回显） | 待补 |
| 6 | 五段式交接摘要示例（真实压缩产出，脱敏后） | 待补 |

现状说明：2026.09.22 尝试在隐藏虚拟桌面打开本机 DSH（`http://127.0.0.1:3080`），页面返回鉴权提示，新开的浏览器配置没有授权 cookie，而启动时打印的带令牌 URL 已不可取（不重启宿主就拿不到）。同日稍早的另一轮验收在 `ALinuxUse/out/` 留下过一批该面板的真机截图，可请仓库作者确认后归档复用，文件名见汇报。

## 候选复用图（尚未归档）

2026.09.22 稍早的另一轮验收在 `ALinuxUse/out/` 留下了同一块面板的真机截图，可作候选：

- `alpha-set-contextzip.png`：ContextZip 面板「已生效」态，副行显示「基于内置 0.1.5-rc.2」，左侧栏已做模糊处理。**这是唯一逐张验过的一张。**
- 其余候选：`alpha-plugins-panel.png`、`alpha-root.png`、`alpha-set-contextzip-save-click.png`、`alpha-set-contextzip-saved.png`、`alpha-settings-page.png`、`alpha-settings.png`、`baseline-settings-scrolled.png`、`baseline-settings-scrolled2.png`、`baseline-settings-scrolled3.png`、`baseline-settings.png`、`r2-dark.png`、`r2-help.png`、`r2-scrolled.png`、`r2-top.png`、`r2-zcp-01-observer-installed.png`、`r2-zcp-02-step1-open-panel.png`、`r2-zcp-03-step1-baseline.png`、`r2-zcp-04-step2-idle-requests.png`、`r2-zcp-05-step3-typed.png`、`r2-zcp-06-step4-server-changed.png`、`r2-zcp-07-step4-panel-caught-up.png`、`r2-zcp-08-step4-unsaved-edit-check.png`、`r2-zcp-09-step5-interval-window.png`、`r2-zcp-10-step6-panel-closed.png`、`r2-zcp-11-step6-after-close-window.png`、`r2-zcp-12-step6-negative-control-reopen.png`、`r2-zcp-13-step6-second-close-window.png`
- 同目录其它图（与本次面板无关或未判定）：`browser.png`、`chip-blue-plugin.png`、`chip-failed.png`、`chip-grey-default.png`、`chip-loading.png`、`ctxzip-displayname-3096.png`、`cz-preview-clean.png`、`cz-preview-dark-full.png`、`cz-preview-dark-full2.png`、`cz-preview-dark-full3.png`、`cz-preview-dark-full4.png`、`cz-preview-dark-scrolled.png`

**归档前必须逐张过一遍**。截图属于发布物，脱敏得按图片做，不能只验一张：

1. 会话标题、工作区路径、会话号、时间戳有没有露；
2. 左侧栏、标签页标题、通知气泡是否已遮挡；
3. PNG 元数据（`tEXt`/EXIF 里可能带工具名与路径）；
4. 图里显示的宿主版本要能在 `dsh-context-zip/package.json` 的 peer 范围里找到。

四项都过，再拷进 `docs/images/` 并在 README 引用；过不了就留在本清单里，不归档。
